---
// ComparisonSection.astro — Container 6 (id: 97edb0a)
// JSON source: page-2.json
//
// COMPUTED MOBILE (iPhone 14, 390px):
//   Section (97edb0a): position:relative, z:auto, overflow:visible, bg:transparent, bgImage:none, padding:0 19.5px
//   Inner (68fa01a): position:relative, z:1, margin:0 0 -70.19px, flexDirection:column
//   Card (b19187b): position:relative, z:1, bg:transparent, borderRadius:0 0 20px 20px, border:0px solid rgba(0,0,0,0.12), padding:35.09px
//   Card overflows section by 70px (mb:-20% on inner)
//
// DESKTOP: gradient bg linear-gradient(304deg, #F8B361, #BB7D34), card bg #F5F1E9, border 1px solid #0000001F, rounded 10px

const problemItems = [
  'Repertório alimentar limitado e sem evolução previsível',
  'Refeições dependentes de negociações constantes',
  'Muitas tentativas, poucas respostas claras',
  'Alimentação ocupando energia mental excessiva',
];

const solutionItems = [
  'Repertório alimentar ampliando de forma progressiva',
  'Refeições integradas à rotina, com menos intervenção e sem desgaste emocional',
  'Decisões alimentares mais claras e seguras',
  'Criança desenvolvendo maior autonomia à mesa',
];

const iconX = 'https://comerbemsemestresse.com.br/wp-content/uploads/2026/02/button.svg';
const iconCheck = 'https://comerbemsemestresse.com.br/wp-content/uploads/2026/02/check-box.svg';
---

<!-- Section: relative, overflow:visible, padding 0 5%, bg transparent mobile / gradient desktop -->
<section
  class="comparison-section relative flex flex-col items-center
    px-[5%] py-0 overflow-visible"
>
  <!-- Inner: relative, z-1, mb:-20% mobile, flex-col -->
  <div class="comparison-inner relative z-[1] w-full max-w-[1280px] mx-auto
    flex flex-col items-center gap-[40px]">

    <!-- Card: relative, z-1, bg transparent mobile / #F5F1E9 desktop -->
    <div
      class="comparison-card relative z-[1] w-full lg:w-[54%]
        rounded-none rounded-b-[20px] md:rounded-[10px]
        p-[10%] md:p-[40px_80px]
        flex flex-col items-center gap-[20px]"
      style="border: 0px solid rgba(0, 0, 0, 0.12);"
    >
      <!-- Heading 1 -->
      <h2 class="font-factul font-normal text-[6.1vw] md:text-[36px] lg:text-[32px] leading-[1.2em] text-[#1C3124] text-center">
        Hoje, <span class="underline-red">sem</span> um sistema alimentar estruturado:
      </h2>

      <!-- Problems -->
      <ul class="w-full flex flex-col gap-[5px]">
        {problemItems.map((item) => (
          <li class="flex items-start gap-[10px]">
            <img src={iconX} alt="" class="w-[22px] h-[22px] mt-[3px] flex-shrink-0" width="22" height="22" />
            <span class="text-[16px] leading-[1.5em] text-[#1C1E20]" style="font-family: Inter, sans-serif;">{item}</span>
          </li>
        ))}
      </ul>

      <!-- Divider -->
      <div class="w-full border-t border-[#0000002B]"></div>

      <!-- Heading 2 -->
      <h2 class="font-factul font-normal text-[6.1vw] md:text-[36px] lg:text-[32px] leading-[1.2em] text-[#1C3124] text-center">
        <span class="underline-verde">Com</span> um método aplicado com constância:
      </h2>

      <!-- Solutions -->
      <ul class="w-full flex flex-col gap-[5px]">
        {solutionItems.map((item) => (
          <li class="flex items-start gap-[10px]">
            <img src={iconCheck} alt="" class="w-[22px] h-[22px] mt-[3px] flex-shrink-0" width="22" height="22" />
            <span class="text-[16px] leading-[1.5em] text-[#1C1E20]" style="font-family: Inter, sans-serif;">{item}</span>
          </li>
        ))}
      </ul>

      <!-- CTA Button: display block = full-width -->
      <a
        href="https://pay.hotmart.com/N90113161T?off=ukulpso1"
        class="block text-[16px] font-semibold text-white uppercase tracking-[0.9px]
          px-[40px] py-[20px] leading-[1.2em] rounded-[1000px]
          text-center"
        style="font-family: Inter, sans-serif; background-image: linear-gradient(to right, #2D8050 0%, #1FBF63 51%, #2D8050 100%); background-size: 200% auto; transition: 0.5s;"
      >
        Quero iniciar o acompanhamento
      </a>

      <!-- Text: Inter 18px/600/italic desktop, 3.8vw mobile -->
      <p class="text-[3.8vw] md:text-[18px] leading-[1.5em] text-[#584C4D] text-center"
        style="font-family: Inter, sans-serif; font-weight: 600; font-style: italic;">
        O Comer bem sem estresse vai lhe dar <span style="color: #D28123;">clareza e direção nessa transformação</span>
      </p>
    </div>

  </div>
</section>

<style>
  /* Mobile: bg transparent, inner mb -20% */
  .comparison-inner {
    margin-bottom: -20%;
  }

  /* Desktop: gradient bg, card bg, border, inner margin reset */
  @media (min-width: 768px) {
    .comparison-section {
      background: linear-gradient(304deg, #F8B361, #BB7D34);
    }
    .comparison-card {
      background-color: #F5F1E9;
      border: 1px solid #0000001F !important;
    }
    .comparison-inner {
      margin-bottom: 0;
    }
  }
</style>
